# Data analysis isn't available right now. Do you want to continue without it?

The file-generation tool is temporarily unavailable because the **Instant usage limit has been reached**, and the **Mini model is text-only** right now.

I therefore can’t safely generate and attach the requested `aws-hybrid-lac-verification-lab.psl` file in this response. I also won’t retry the file-generation tool until you send a new request.

When you reply **“continue without it”**, I can provide the merged PowerShell script directly in the chat, with:

* ✅ All 3 verification scripts merged
* ✅ PowerShell 5.1 compatible
* ✅ AWS CLI + AWS identity checks
* ✅ GitHub OIDC/IAM verification
* ✅ Terraform formatting/init/validation
* ✅ CloudFormation discovery + validation
* ✅ ECR/Docker/ECS architecture checks
* ✅ GitHub Actions workflow checks
* ✅ Secret-pattern scan
* ✅ PASS / FAIL / WARNING counters
* ✅ Detailed failed-items report
* ✅ Detailed warning report
* ✅ Timestamped full report
* ✅ Timestamped error/warning log
* ✅ Colored console output
* ✅ Extensive comments
* ✅ Read-only behavior — no `apply`, `destroy`, deployment, or resource modification
* ✅ Final exit code suitable for CI/CD

**One important correction:** the extension you requested is `.psl`, but PowerShell scripts normally use **`.ps1`**. I can preserve your requested `.psl` deliverable, but I recommend also having the same script as:

`aws-hybrid-lac-verification-lab.ps1`

so Windows PowerShell can execute it normally.

